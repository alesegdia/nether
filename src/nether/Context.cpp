#include "nether/Context.h"

#include "nether/Texture.h"
#include "nether/ShaderProgram.h"

#include <vector>

/*

namespace
{
	std::vector<nether::Texture> textures;
	std::vector<nether::ShaderProgram> shaderPrograms;

}

namespace nether
{

	void init()
	{
	}

	void exit()
	{
	}

	Texture* newTexture(const std::string& path)
	{
		textures.emplace_back(path);
		return &textures.back();
	}

	ShaderProgram* newShaderProgram(const std::string& vspath, const std::string& fspath)
	{
		shaderPrograms.emplace_back(vspath, fspath);
		return &shaderPrograms.back();
	}
}
*/